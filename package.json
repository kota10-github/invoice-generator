{
  "name": "invoice-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Invoice Generator - PDF invoice creation tool",
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'Static site - no build needed'"
  }
}
